{
    "name": "spacedom",
    "private": true,
    "version": "0.0.0"
    "scripts": {
        "b": "pnpm --filter @spacedom/backend",
		"w": "pnpm --filter @ideanick/webapp",
		"dev": "pnpm -r --parallel dev",
		"types": "concurrently 'pnpm b types' 'pnpm w types'",
		"prettify": "pnpm -r --parallel prettify"
    }
}